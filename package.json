{
  "name": "themildtake-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "pack-data": "node scripts/pack-data.mjs",
    "data": "npm run sync-data && npm run pack-data",
    "validate": "node scripts/validate-data.mjs",
    "analyze-bias": "node scripts/analyze-bias.mjs",
    "dev": "npm run data && astro dev",
    "start": "npm run dev",
    "prebuild": "npm run validate && npm run data",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "npm run validate && node --test"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/preact": "^4.0.0",
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.2.0",
    "@msgpack/msgpack": "^3.1.3",
    "astro": "^5.0.0",
    "echarts": "^5.5.0",
    "fast-xml-parser": "^5.8.0",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
